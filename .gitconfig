[alias]
    cm = commit --cleanup=whitespace --no-status
    graph = log --graph --all --oneline --decorate
    mg = "!f() { [ $2 ] && git checkout $1 && git merge $2; }; f"
    svncm = svn dcommit --rmdir
    ur = branch --no-track -f

[color]
    ui = auto

[core]
    autocrlf = false
    editor = vim
    whitespace = cr-at-eol

[diff]
    tool = vimdiff

[push]
    default = upstream

[user]
    name = sgm
    email = sim0629@gmail.com
